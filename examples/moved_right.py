"""rox-merge moved-block 데모 (우측): helper 함수가 파일 아래쪽으로 이동했다."""

config_a = 1
config_b = 2
config_c = 3
config_d = 4
print(config_a, config_b, config_c, config_d)


def helper(value):
    doubled = value * 2
    return doubled
