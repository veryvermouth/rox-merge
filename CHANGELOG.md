# 변경 이력 (Changelog)

이 프로젝트의 주요 변경 사항을 기록한다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를
따르고, 버전은 [유의적 버전](https://semver.org/lang/ko/)을 지향한다.

## [0.2.0]

### 추가
- **드래그 & 드롭**: 파일 비교 창에 파일을, 폴더 비교 창에 폴더를 끌어다 놓아 바로 열기(2개 → 좌/우, 1개 → 놓은 위치 기준 좌/우).
- **폴더 비교 경로 바**: 폴더 탭 상단에 좌/우 폴더 경로 입력칸 + `...` 찾아보기. 포커스 시 **최근 폴더 자동완성 드롭다운**(전역·저장, 최근 파일과 별개).
- **빈 화면 웰컴 화면**: 모든 탭을 닫으면 빈 파일 탭을 자동 생성하는 대신 **"ROX-MERGE" 로고 화면**(그라데이션 타이틀 + 시작 안내)을 표시.
- **도움말 > 단축키(F1)**: 현재 단축키와 설명을 카테고리별로 보여주는 창.

### 변경
- **폴더 트리 글꼴**을 맑은 고딕(Malgun Gothic)으로 지정 — 파일/폴더 이름의 한글+영문 가독성 향상.
- 폴더 탭 내부 탭바("폴더" 라벨)를 **자동 숨김**: 폴더 페이지 하나뿐일 땐 감추고, 파일 diff 새 탭이 열리면 표시.

## [0.1.0]

### 핵심 (Phase 0–5)
- **Phase 0** — 프로젝트 뼈대(src 레이아웃), 파일 I/O(인코딩·줄바꿈·바이너리 감지, 바이트 보존 읽기/쓰기), pytest.
- **Phase 1** — Diff 엔진: Myers 라인 diff, side-by-side 정렬(gap), 단어 단위 intraline, 공백만 다른 줄(whitespace) 분류, hunk 그룹화. replace 블록은 유사도 기반(NW) 정렬.
- **Phase 2** — 파일 비교 GUI(커스텀 페인팅): 색상·intraline, 스크롤 동기화, 차이 점프, 미니맵, 글꼴 줌.
- **Phase 3** — 병합 버튼(→/←) + ApplyHunk 커맨드, 직접 텍스트 편집, Undo/Redo 통합 스택, 편집 후 150ms debounce 재계산.
- **Phase 4** — Moved block 탐지(해시, 최소 3라인·기본 ON): 전용 보라색 + 좌↔우 연결선, 토글.
- **Phase 5** — 폴더 비교: 재귀 비교 엔진(동일/좌측만/우측만/내용 다름), 빠른(크기+mtime)/정확(해시) 토글, 좌/우 대응 트리, 다른 항목만 필터, 차이 있는 곳만 초기 확장, 분할 ↔ 새 탭 모드.

### Phase 6 — 다듬기
- 비교 옵션 UI: **공백 무시 / 대소문자 무시** 토글(엔진 연동).
- **큰 파일 가드**: 총 라인 수 초과 시 이동 탐지 자동 비활성 / diff 생략(플레인), 상태바 안내.
- **다크 테마**: 라이트/다크 전역 토글. Windows 네이티브 스타일이 팔레트를 무시하는 문제로 **Fusion 스타일 + 명시적 팔레트**를 써서 OS 테마와 무관하게 메뉴바·툴바·트리까지 전환.
- 라이트 테마 색을 **Araxis 계열**(변경=크림, 추가=초록, 삭제=분홍)로 조정.

### UI 구조
- **탭 단위 비교 세션**으로 통합: 새 창 대신 하나의 창에서 파일/폴더 비교 탭 추가.
- **메뉴바 + 간단 툴바**: 전체 기능을 메뉴로, 자주 쓰는 것만 툴바로. 현재 활성 탭에 맞춰 항목 활성/비활성.
- 실행 중 파일↔폴더 비교 상호 전환.

### 파일 비교
- 좌/우 상단 **경로 입력 바** + `...` 찾아보기 + 포커스 시 **최근 파일 자동완성 드롭다운**(전역·저장).
- `새 파일 비교`는 좌→우 파일 선택 시퀀스로 시작. 개별 열기는 경로칸 `...` 버튼으로.
- **텍스트 선택**(Shift·드래그·Ctrl+A) + 복사/잘라내기/붙여넣기, **한글 등 IME 입력**.
- gap(공백줄) 쪽도 diff 종류색으로 강조. Enter 등 구조적 편집은 즉시 재계산.

### 폴더 비교
- 트리를 **좌/우 대응 2열**로: 이름 들여쓰기 + 펼침/접힘 화살표를 양쪽에 미러링. **오른쪽 열 화살표 클릭으로도 펼치기/접기**.

### 글꼴·테마·설정
- 글꼴 크기: **파일 diff 글꼴**(파일 탭·폴더 하단 diff 공유)과 **폴더 트리 글꼴**을 따로 관리. 확대/축소·리셋은 **커서 포커스 기준**. 트리 기본 10pt.
- **설정 저장/복원(QSettings)**: 글꼴 크기, 다크 테마, 비교 옵션, 최근 파일.

### 단축키
- 차이 점프 `Ctrl+2/3` → **`Ctrl+1/2`**.
- 탭 닫기 `Ctrl+W` 에 **`Ctrl+F4`** 추가.
- `Ctrl+0` = 글꼴 원래 크기(포커스 기준), 트리 초기 상태는 **`Ctrl+Shift+0`**.
- 새 파일/폴더 비교 `Ctrl+N`/`Ctrl+D`, 복사/붙여넣기/모두 선택 등 편집 단축키.

### 문서
- README를 현재 구현 상태로 전면 갱신(기능·단축키·구조). PLAN.md는 초기 계획서로 유지.

### 테스트
- 순수 로직(diff 엔진·커맨드·폴더 비교·게이팅·가드)과 헤드리스 GUI(편집·옵션·테마) 단위 테스트 **194개**.
